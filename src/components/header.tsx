import '../styles/header.scss'

const Header = () => {
  return (
    <header className="siteHeader">
        <img src="/images/logo-blue.png" alt="Logo" className="siteLogo" />
    </header>
  );
}

export default Header;
